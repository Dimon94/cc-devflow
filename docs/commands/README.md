# Command Reference Retired

CC-DevFlow no longer treats commands as the user-facing interface.

Use skills directly:

```text
roadmap

req-plan -> req-do -> req-check -> req-act
```

Install the skill pack with [skills.sh CLI](https://skills.sh/docs/cli):

```bash
npx skills add Dimon94/cc-devflow
```
